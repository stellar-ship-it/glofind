# 글로파인드 — 디자인 토큰 (실측 확정)

> 2026-08-29. hucord.com · goodai-global.com · ancors.co.kr **3곳을 브라우저로 열어
> 렌더된 computed style을 실측**해 확정한 값이다. 감으로 조정하지 않는다.
> 수치를 바꾸려면 레퍼런스를 다시 재고 이 표를 갱신한 뒤 바꾼다.

## 실측 원본

| 축 | hucord | goodai | ancors | 채택 |
|---|---|---|---|---|
| 본문 폰트 | Pretendard | Pretendard | Pretendard | Pretendard (만장일치) |
| 타입 단계·대비 | 13 · 5.7배 | 9 · 22.5배 | 13 · 7.1배 | 12 · 9배 |
| 최대 헤드라인 | 86px | 293px | 78px | 96px |
| 웨이트 | 400우세·700소수 | 400:409/700:13 | 400:229/700:115 | 본문 400 · 제목 500~600 |
| 라운드 종류 | 3 (원형·pill) | **0** | 1 (원형) | **0 + 원형만** |
| 그림자 종류 | **0** | **0** | **0** | **0** |
| 최대 채도 | S100 #009944 | **S0** | S81 #E62318 | 유지·사용처 제한 |
| 배경 | — | #F5F5F5 / #141414 | #FFF / #000 | 화이트 + 진짜 블랙 |
| 컨테이너 | 1840 | 1280 | 1200 | 1280 |
| 헤더 | fixed·투명·blur없음 | sticky·투명·blur없음 | fixed·투명 | 투명 · blur 없음 |
| duration | 1s/.7s/.5s | .4s/.2s | .25s/.7s/1.2s | UI .25s · 리빌 .7s |
| 이징 | — | ease-out | cubic-bezier(.22,1,.36,1) | cubic-bezier(.22,1,.36,1) |

**가져오지 않은 것**: 로고 · 사진 · 카피 · 전체 레이아웃 · hex값(#009944, #E62318).
측정한 것은 수치 체계뿐이다.

## 금지 조항 — 이유와 함께

1. **`box-shadow` 금지.** 레퍼런스 3곳 모두 0종이다. 경계는 `1px solid var(--line)`으로 낸다.
   그림자를 쓰는 순간 부트스트랩 시대 카드로 후퇴한다.
   → 유일한 예외: `#nav.scrolled` 하단 헤어라인(그림자가 아니라 border).
2. **`border-radius` 금지.** 원형(`50%`)과 pill(`999px`)만 허용한다.
   4/10/16/24px 라운드가 카드에 붙는 순간 "AI가 만든 SaaS 랜딩"이 된다.
3. **브랜드 시안(`--cyan`)을 면에 깔지 않는다.** 선·점·작은 라벨·아이콘에만 쓴다.
   ancors는 S81 레드를 점으로만 쓴다. 그라디언트 버튼·글로우·페일 배경 전부 금지.
   → 밝은 배경 위 텍스트에는 `--cyan-ink`(#00727C)만 쓴다. `--cyan`은 대비 2.6:1로 미달이다.
4. **`font-weight: 700` 이상을 본문·중제목에 쓰지 않는다.** 본문 400, 중제목 500, 대제목 600,
   디스플레이만 700. 3곳 모두 400이 압도적이고 700을 아껴 쓴다.
5. **그라디언트 배경 금지.** 단색 면 + 1px 선으로 낸다. 질감은 그레인 오버레이가 담당한다.
6. **한 섹션 1.5화면(1350px @900vh) 초과 금지.**
7. **인접 섹션은 격자를 바꾼다.** 배경만 번갈아서는 부족하다.
8. **한글은 `word-break: keep-all`.** 제목 줄바꿈은 `<br>`로 직접 잡는다.

## 색 체계 — 와디즈 실측 (2026-09-03 갱신)

> 클라이언트 지시: "색감이 칙칙하다. 와디즈와 포인트 색이 비슷하니 컬러 시스템을 참고하라."
> wadiz.kr 을 브라우저로 열어 computed style 빈도를 실측했다. **hex 는 글로파인드 것을 유지**하고
> 회색 온도와 '시안을 쓰는 방식'만 가져왔다.

| 축 | 와디즈 실측 | 채택 |
|---|---|---|
| 잉크 | #191F28 (4,225회) · #4E5968 · #6B7684 · #8B95A1 | 그대로 — 블루 틴트 그레이 스케일 |
| 면 | 흰색 + #F2F5F8 (521회) + #FAFBFD | 흰색 기본 · 쿨 그레이 밴드 · 오프화이트 카드 |
| 브랜드 | #00C4C4 — primary 버튼 면(흰 글자) · 텍스트 396회 · 틴트 #E6FAFA | 글로파인드 #00ADBD 유지. **버튼 면 허용** · 틴트 면 1곳 허용 |
| 보조 | 블루 #4672F9 · 옐로 · 레드 | 가져오지 않음 (B2B 단일 포인트 유지) |
| 라운드 | 4px 버튼 · 8px 칩 | 가져오지 않음 (0 유지) |

**칙칙함의 원인은 회색의 온도였다.** 중성 회색(#111/#7A7A7A/#F5F5F5) 위의 시안은 탁하고,
블루 틴트 회색 위의 시안은 맑다. 그래서 금지 조항 3을 아래처럼 완화한다.

3'. **시안 면은 primary 버튼과 틴트 밴드 1곳에만.** 그라디언트·글로우는 여전히 금지.
   시안 면 위 글자는 `--on-cyan`(흰색). 밝은 면 위 시안 텍스트는 `--cyan-ink`.

## 토큰

```css
--ink:#191F28; --ink-70:#4E5968; --ink-50:#6B7684; --ink-30:#8B95A1;
--line:#E5E8EB; --line-2:#F2F5F8;
--surface:#FFFFFF; --bg:#F2F5F8; --bg-2:#FAFBFD; --tint:#E6FAFA;
--black:#191F28; --black-2:#10141B;
--cyan:#00ADBD; --cyan-hover:#009AA9; --cyan-ink:#007F8B; --on-cyan:#FFFFFF;
--w-content:1280px;
--fs-display:clamp(56px,8.4vw,120px);  --fs-h2:clamp(36px,4.2vw,60px);  --fs-body:17px;
--radius:0; --radius-pill:999px; --radius-circle:50%;
--shadow:none;
--e:cubic-bezier(.22,1,.36,1);  --d-ui:250ms;  --d-reveal:700ms;
```

## 소형 타입 3벌 (2026-09-04 감수 · 2026-09-11 클라이언트 지시로 1px씩 상향)

16px 미만 글자는 아래 세 벌만 쓴다. 새 크기·자간·굵기를 만들지 않는다.

| 역할 | 값 | 색 (밝은 면 / 다크) | 쓰는 곳 |
|---|---|---|---|
| 보조 본문 | 16px · 400 · Pretendard · −.012em | ink-70 / on-black-70 | 푸터 링크, 목록, 우측 보조 문장, 숫자 단위 |
| 라벨·캡션 | 14px · 500(라벨) / 400(캡션) · Pretendard · −.012em | ink-50 / on-black-70 | 섹션 라벨, 태그, 지표 캡션, 폼 라벨, 표 머리, 에러, 저작권 |
| 영문 마이크로 | 12px · 600 · Montserrat · .16em · 대문자 | ink-50 / on-black-50 · 강조 cyan | 히어로 라벨, 푸터 컬럼 제목·태그·시계, 스크롤 힌트, 카운터, 스텝 번호 |

13px·11px·10px·.3em 같은 파생 크기는 폐기했다. 섹션 제목(--fs-h2)은 60px 상한 — 히어로(92~120px)와 위계를 분리하기 위해 2026-09-11 80→60으로 내렸다. 굵기 700 이상은 소형 글자에 쓰지 않는다.

## 파일 구성

- `assets/css/tokens.css` — 위 토큰. 다른 파일에서 hex/px를 직접 쓰지 않는다
- `assets/css/site.css` — 컴포넌트 · `assets/css/pages/*.css` — 서비스·인사이트 페이지 전용 (구 main.css 는 2026-09-14 제거)
- `assets/css/motion.css` — 모션·질감 레이어
- `assets/js/motion.js` — Tier 1 모션 (리빌·패럴랙스·자기력·상하이동·커서·단어 분해)
- `assets/js/motion2.js` — Tier 2 모션 (GSAP ScrollTrigger + Lenis: 히어로 스크럽·프로세스 진행선·티커 속도·호버 이미지) — 가드로 감싸 미로드 시 Tier 1만 동작
- `admin/` — 관리자(PHP). 스타일은 `admin/assets/css/admin.css` 한 파일, 토큰은 `tokens.css` 를 그대로 읽는다. 위 금지 조항이 관리자에도 적용된다
- `admin/lib/publisher.php` + `admin/templates/` — 인사이트 정적 생성. 공개 아티클 마크업을 바꾸려면 `insights/*/index.html` 이 아니라 여기를 고치고 '전체 재발행' 한다
- `api/` — 공개 엔드포인트(상담 폼 접수 · 방문 비컨) · DB 래퍼. 자격증명은 `api/config.local.php`(Git 제외)에만
